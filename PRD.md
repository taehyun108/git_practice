# 드럼 연습용 오디오 분리 프로그램 PRD

상태: 초안  
작성 기준: 사용자가 제공한 아이디어 정리 문서  
대상 환경: Windows 데스크톱

> 이 문서는 제공된 자료에서 확인할 수 있는 내용만 요구사항으로 정리한 초안이다. 자료에 없는 제품 정책과 수치값은 임의로 결정하지 않고 `미정` 또는 `질문`으로 표시한다.

## 1. 제품 개요

사용자가 보유했거나 다운로드 권한을 가진 음원 파일 또는 YouTube URL을 입력하면, 음원을 분석해 드럼 연습에 사용할 두 개의 MP3를 자동으로 생성하는 Python 기반 Windows 프로그램이다.

생성물은 다음과 같다.

- 드럼만 포함한 트랙: `<곡명>_drums.mp3`
- 드럼을 제거한 연습용 MR: `<곡명>_no_drums.mp3`

## 2. 문제와 목표

### 해결하려는 문제

드럼 연습을 위해 원곡에서 드럼 트랙과 드럼이 제거된 반주를 별도로 준비하는 작업이 번거롭다.

### 제품 목표

1. 로컬 음원 또는 권한이 있는 YouTube URL을 일괄 입력할 수 있게 한다.
2. Demucs 기반 음원 분리로 드럼 및 드럼 제거 트랙을 자동 생성한다.
3. CPU와 CUDA GPU 환경 모두에서 동작하게 한다.
4. Windows 사용자가 배치 파일을 실행하는 방식으로 설치·처리할 수 있게 한다.
5. 한 곡의 오류가 전체 작업을 중단시키지 않게 한다.
6. 처리 결과와 오류를 파일 로그로 남긴다.

### 범위에 포함하지 않는 것

- 저작권이 불명확한 콘텐츠의 다운로드
- DRM, 로그인, 쿠키, 연령·지역·유료 제한 등의 우회
- 원본 영상 저장
- 제공 자료에 명시되지 않은 별도 GUI 애플리케이션
- 제공 자료에 명시되지 않은 오디오 편집 기능

## 3. 대상 사용자와 사용 시나리오

### 대상 사용자

Windows에서 드럼을 연습하며 개발 지식 없이 음원 분리 결과를 얻고 싶은 사용자.

### 핵심 시나리오

#### 로컬 음원 처리

1. 사용자가 `input/` 폴더에 MP3, WAV, FLAC 또는 M4A 파일을 넣는다.
2. 사용자가 `run.bat`을 실행한다.
3. 프로그램이 입력 파일을 검색하고 곡별로 처리한다.
4. 사용자가 `output/<곡명>/`에서 두 개의 MP3를 확인한다.

#### 권한이 있는 YouTube URL 처리

1. 사용자가 `youtube_urls.txt`에 한 줄당 하나의 URL을 입력한다.
2. 사용자가 `run.bat`을 실행한다.
3. 프로그램이 허용된 방식으로 오디오만 임시 다운로드한다.
4. 프로그램이 다운로드한 오디오를 분리하고 결과를 저장한다.
5. 임시 파일을 정리한다.

#### 로컬 파일과 URL 동시 처리

`input/`의 로컬 파일과 `youtube_urls.txt`의 URL을 한 번의 실행에서 함께 처리할 수 있어야 한다.

## 4. 입력 및 데이터

### 로컬 입력

지원 확장자:

- `.mp3`
- `.wav`
- `.flac`
- `.m4a`

검색 위치:

```text
input/
```

### YouTube 입력

파일:

```text
youtube_urls.txt
```

규칙:

- 한 줄에 URL 하나를 입력한다.
- 사용자가 직접 제작·소유했거나 다운로드·2차 이용 권한이 있는 콘텐츠만 대상으로 한다.
- 다운로드가 정상적으로 허용되지 않는 URL은 건너뛰고 로그에 기록한다.
- 원본 영상은 저장하지 않는다.
- 필요한 오디오만 임시 폴더에 저장하고 처리 후 삭제한다.
- 쿠키 자동 추출, 인증 우회, DRM 해제 및 접근 제한 우회는 지원하지 않는다.

### 곡 메타데이터

파일:

```text
songs.json
```

기본 구조는 곡의 `title`, `artist`, `source`, `url` 필드를 포함하는 JSON 배열이다. 제공 자료에는 예시 구조만 있으며, 실제 처리 시 이 파일을 입력 목록·메타데이터 관리에 어떻게 사용할지는 미정이다.

## 5. 기능 요구사항

### FR-01. 입력 수집

- 프로그램은 `input/`의 지원 확장자 파일을 검색해야 한다.
- 프로그램은 `youtube_urls.txt`가 존재하고 유효한 URL이 있으면 처리 대상으로 수집해야 한다.
- 로컬 파일과 URL을 동시에 처리해야 한다.
- `--input "song.mp3"` 사용 시 지정한 한 곡을 처리할 수 있어야 한다.
- `--url "AUTHORIZED_URL"` 사용 시 지정한 URL을 처리할 수 있어야 한다.

### FR-02. 음원 분리

- Python 기반으로 구현한다.
- Demucs를 음원 분리 모델로 사용한다.
- 기본 분리 스템은 `vocals`, `drums`, `bass`, `other` 네 가지다.
- 드럼 트랙은 `drums` 스템만 사용한다.
- 드럼 제거 MR은 `vocals + bass + other`를 합성한다.

### FR-03. 오디오 후처리 및 인코딩

- 합성 과정에서 클리핑이 발생하지 않도록 peak normalization 또는 적절한 gain 처리를 해야 한다.
- 최종 출력 형식은 MP3다.
- 기본 비트레이트는 320 kbps다.
- 채널은 Stereo다.
- 샘플레이트 정책은 `원본 유지`와 `44.1 kHz` 중 미정이다.
- FFmpeg를 사용해 MP3를 생성한다.
- FFmpeg가 없으면 원인을 사용자가 이해할 수 있는 오류 메시지로 안내하고 로그에 남긴다.

### FR-04. GPU 및 실행 장치

- CUDA를 사용할 수 있는 NVIDIA GPU가 있으면 자동으로 GPU를 사용한다.
- CUDA를 사용할 수 없으면 CPU를 사용한다.
- `--device cpu`로 CPU를 강제할 수 있어야 한다.
- `--device cuda`로 CUDA 사용을 지정할 수 있어야 한다.
- 실행 시 선택된 장치와 모드를 콘솔에 표시한다.
- `--device cuda`를 지정했지만 CUDA를 사용할 수 없는 경우의 처리 정책은 미정이다.

### FR-05. 파일명 및 출력 구조

출력 구조:

```text
output/
└─ <곡명>/
   ├─ <곡명>_drums.mp3
   └─ <곡명>_no_drums.mp3
```

다음 문자는 Windows 파일명에서 제거하거나 안전한 문자로 치환해야 한다.

```text
\ / : * ? " < > |
```

곡명은 YouTube 제목 또는 로컬 파일명에서 정해지는 것으로 제공 자료에 적혀 있으나, 두 입력의 우선순위와 중복 곡명 처리 방식은 미정이다.

### FR-06. 중복 처리

- 동일 곡의 `_drums.mp3`와 `_no_drums.mp3`가 모두 존재하면 기본적으로 다시 처리하지 않는다.
- 이 경우 콘솔에 `[SKIP] 이미 처리된 곡입니다.`를 출력한다.
- `--force` 사용 시 기존 결과를 삭제하고 다시 처리한다.
- 두 결과 중 하나만 존재하는 경우의 처리 정책은 미정이다.

### FR-07. 오류 격리 및 로그

- 한 곡에서 오류가 발생해도 다음 곡의 처리를 계속한다.
- 오류 발생 시 곡명, 원인, 다음 곡으로 진행한다는 안내를 콘솔에 출력한다.
- 로그 파일은 다음 위치에 저장한다.

```text
logs/app.log
```

- 로그에는 시간, 곡명, 입력 타입, 성공·실패, 처리 시간, 오류 메시지를 기록한다.

### FR-08. 임시 파일 정리

- 다운로드 및 분리 중간 파일은 `temp/` 아래에 저장한다.
- 성공적으로 처리한 뒤 임시 파일을 삭제한다.
- 오류가 발생해도 가능한 범위에서 임시 파일을 정리한다.
- 프로그램이 강제 종료되거나 시스템 장애가 난 뒤 남은 임시 파일의 처리 정책은 미정이다.

### FR-09. 설치 및 실행

`setup.bat`은 다음 준비 작업을 수행해야 한다.

1. Python 설치 여부 확인
2. 가상환경 생성
3. 가상환경 활성화
4. pip 업그레이드
5. requirements 설치
6. FFmpeg 존재 여부 확인
7. PyTorch 및 CUDA 확인
8. Demucs 설치 여부 확인

완료 시 `설치가 완료되었습니다. run.bat을 실행하세요.`를 표시한다.

`run.bat`은 Python 명령을 직접 입력하지 않아도 가상환경을 활성화한 뒤 `main.py`를 실행해야 한다. 실행 종료 시 결과 경로를 표시해야 한다.

### FR-10. CLI

지원 대상:

```text
python main.py
python main.py --input "song.mp3"
python main.py --url "AUTHORIZED_URL"
python main.py --force
python main.py --device cpu
python main.py --device cuda
```

인자의 조합 규칙, 우선순위 및 잘못된 인자에 대한 종료 코드는 미정이다.

### FR-11. 사용자 문서

`README.md`는 한국어로 작성하며 다음을 비개발자도 따라 할 수 있게 설명해야 한다.

- 최초 설치: `setup.bat` 실행
- 로컬 음원 사용: `input/`에 음원 추가 후 `run.bat` 실행
- 허가된 YouTube URL 사용: `youtube_urls.txt`에 URL 추가 후 `run.bat` 실행
- `_drums.mp3`와 `_no_drums.mp3`의 차이
- Windows용 FFmpeg 설치 방법

## 6. 예상 워크플로우

아래 흐름은 제공된 텍스트에서 확인되는 처리 순서를 정리한 것이다. 원본 워크플로우 그림의 세부 단계·분기와 다를 경우 그림 확인 후 수정해야 한다.

```text
프로그램 시작
    │
    ├─ 실행 옵션 읽기
    ├─ 처리 장치 결정(CUDA 또는 CPU)
    ├─ 로컬 파일 수집(input/)
    └─ URL 수집(youtube_urls.txt 또는 --url)
            │
            ▼
      처리 대상별 반복
            │
            ├─ 입력 확인 및 곡명 결정
            ├─ Windows 안전 파일명 생성
            ├─ 기존 결과 확인
            │     ├─ 결과 2개 존재 + --force 없음 → SKIP
            │     └─ 그 외 → 처리 계속
            ├─ URL이면 오디오만 temp/에 다운로드
            ├─ 오디오 준비
            ├─ Demucs로 vocals/drums/bass/other 분리
            ├─ drums 스템으로 드럼 트랙 생성
            ├─ vocals+bass+other로 no-drums 트랙 생성
            ├─ 클리핑 방지 후 MP3 인코딩
            ├─ output/<곡명>/에 결과 저장
            ├─ 처리 결과 및 시간 로그 기록
            └─ temp/ 중간 파일 정리
            │
            ├─ 성공 → 다음 곡
            └─ 오류 → 오류 로그 기록·정리 후 다음 곡
            │
            ▼
       전체 처리 종료 및 결과 경로 표시
```

## 7. 프로젝트 구성

권장 기본 구조:

```text
drum_extractor/
├─ input/
├─ output/
├─ temp/
├─ logs/
├─ main.py
├─ separator.py
├─ audio_utils.py
├─ downloader.py
├─ config.py
├─ songs.json
├─ youtube_urls.txt
├─ requirements.txt
├─ run.bat
├─ setup.bat
├─ .gitignore
└─ README.md
```

코드 규모에 따라 파일을 통합할 수 있으나, 입력 수집·다운로드·분리·오디오 변환·실행 제어의 책임은 유지보수하기 쉽게 분리한다.

## 8. 비기능 요구사항

### NFR-01. 운영 환경

- Windows에서 실행되어야 한다.
- CPU 환경에서도 정상 처리되어야 한다.
- NVIDIA GPU와 CUDA가 정상 설치된 환경에서는 GPU를 활용해야 한다.
- 한글 곡명과 Windows 경로를 안전하게 처리해야 한다.

### NFR-02. 안정성

- 입력 한 곡의 실패가 전체 배치 실패로 이어지지 않아야 한다.
- 임시 파일과 부분 결과가 남을 수 있는 실패 상황을 고려해야 한다.
- 외부 실행 파일(FFmpeg) 및 모델 실행 오류를 사용자에게 명확히 알린다.

### NFR-03. 재현성과 설치성

- `requirements.txt`에는 실제 사용 라이브러리만 포함한다.
- Windows Python 환경에서 충돌 가능성이 낮은 버전을 사용한다.
- Demucs 모델 버전과 모델 다운로드·캐시 정책은 미정이다.

### NFR-04. 저작권 및 보안

- 사용자가 다운로드 권한을 가진 콘텐츠만 처리 대상으로 안내한다.
- 접근 제한·DRM·로그인·쿠키 인증을 우회하지 않는다.
- URL 처리 중 원본 영상을 저장하지 않는다.

## 9. 성공 기준 및 검수 항목

### 기능 검수

- 지원하는 로컬 확장자를 검색한다.
- URL 목록과 로컬 파일을 함께 처리한다.
- Demucs의 네 개 스템을 사용해 두 종류의 MP3를 만든다.
- 드럼 트랙은 `drums`만 포함한다.
- no-drums 트랙은 `vocals`, `bass`, `other`를 합친다.
- Windows 금지 문자가 있는 곡명도 정상적인 폴더·파일명으로 저장한다.
- 이미 두 결과가 있으면 건너뛴다.
- `--force`로 다시 처리할 수 있다.
- CPU 및 CUDA 장치 선택이 동작한다.
- FFmpeg 오류를 안내하고 로그에 남긴다.
- 한 곡 실패 후 다음 곡을 처리한다.
- 성공·실패 후 임시 파일을 가능한 범위에서 정리한다.

### 품질·실행 검수

- Python syntax 오류가 없다.
- import 오류가 없다.
- Windows 경로와 한글 파일명이 동작한다.
- FFmpeg 호출과 MP3 생성이 동작한다.
- Demucs 결과 경로를 올바르게 읽는다.
- 실제 음원이 없는 환경에서도 경로·인자·중복·오류 처리 등 테스트 가능한 부분을 검증한다.
- 실제 음원을 사용한 전체 실행 테스트 여부와 기준은 미정이다.

## 10. 미정 사항 및 질문

아래 항목은 제공된 자료만으로 확정할 수 없어 구현 전에 답이 필요하다.

1. 워크플로우 그림 원본을 공유해 줄 수 있는가? 현재 제공된 텍스트에는 그림의 세부 단계·분기·화면 요소가 포함되어 있지 않다.
2. 지원할 Windows 최소 버전과 Python 버전 범위는 무엇인가?
3. 출력 샘플레이트는 원본 유지와 44.1 kHz 중 어떤 정책을 원하는가?
4. 음량 정규화 시 목표 headroom 또는 허용되는 음량 변화 기준이 있는가?
5. `songs.json`은 실제 처리 목록으로 사용할 것인가, 아니면 예시·곡 메타데이터 보관용인가?
6. 로컬 파일의 곡명은 확장자를 제거한 파일명으로 할 것인가? YouTube 제목과 `songs.json`의 제목이 다를 때 어떤 값을 우선할 것인가?
7. sanitize 후 서로 다른 곡명이 같은 폴더명으로 충돌하면 어떻게 처리할 것인가?
8. 두 출력 파일 중 하나만 이미 존재할 때 기본 동작은 전체 재처리, 누락 파일만 생성, 오류 중 무엇인가?
9. `--force`가 기존 결과를 덮어쓸 때 백업 파일을 만들 필요가 있는가?
10. `--input`, `--url`, 기본 일괄 처리 옵션을 함께 지정했을 때 모두 처리할 것인가, 특정 입력을 우선할 것인가?
11. `--device cuda`를 지정했지만 CUDA가 불가능하면 즉시 실패할 것인가, CPU로 자동 전환할 것인가?
12. 처리 진행률은 현재 예시처럼 콘솔 출력만 필요한가? 별도 GUI 또는 진행률 파일이 필요한가?
13. 처리 중 일부 파일만 생성된 경우 해당 부분 결과를 삭제하고 다음 곡으로 넘어갈 것인가?
14. 로그 파일의 보존 기간·최대 크기·인코딩 정책이 필요한가?
15. 강제 종료 후 `temp/`에 남은 파일을 다음 실행 시 자동 정리할 것인가?
16. 실제 음원을 이용한 검수용 테스트 파일과 허용 가능한 처리 시간·품질 기준이 있는가?

## 11. 후속 결정 기록

질문에 대한 답변이 정해지면 이 문서의 `미정` 항목과 수용 기준을 갱신한다.
